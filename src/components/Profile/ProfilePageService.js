// import getUserEmail from '../../components/header/HeaderService';

// // if (getUserEmail() === true) {

// // }
// Check if the user is logged in with an email
// Make sure
// the email signed in recieves the correct data from the database.
// export default async function fetchAddress(setAddress, setApiError) {
//     await Httphelper(Constants.)
//
