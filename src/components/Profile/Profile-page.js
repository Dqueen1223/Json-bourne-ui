import React, { useEffect, useState } from "react";
// import app from ''
// Display First Name

// Last Name

// Shipping Address:

// Street

// City

// State

// Zip

/**
 *
 *
 *
 */

const ProfilePage = () => {
    console.log('works');
    const ShippingButton = () => { 
        this.setState(prevstate => ({
            
        }))
    };
    
  return (
    // eslint-disable-next-line react/react-in-jsx-scope
      <button onClick={this.ShippingButton}>
          Shipping
      </button>
  );
};
export default ProfilePage;


// import data  associated with email from delivery address
// how do i access that data?
// 1.Possibly setup a query endpoint
// 2.get
